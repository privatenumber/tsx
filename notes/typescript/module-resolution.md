# Module resolution

Module resolution searches TypeScript, declaration, JavaScript, and, when enabled, JSON extensions independently of `allowJs` ([resolver extension masks](https://github.com/microsoft/TypeScript/blob/b465fdbfe175304d9b977da137b2c178ae1091d3/src/compiler/moduleNameResolver.ts#L1738-L1795)); the program builder separately decides which discovered JavaScript files become inputs ([filtering](https://github.com/microsoft/TypeScript/blob/b465fdbfe175304d9b977da137b2c178ae1091d3/src/compiler/program.ts#L3929-L3957)).

Known emitted extensions are substituted before implicit extensions are appended: `./value.js` can resolve to `value.ts`, `value.tsx`, `value.d.ts`, then `value.js` ([candidate table](https://github.com/microsoft/TypeScript/blob/b465fdbfe175304d9b977da137b2c178ae1091d3/src/compiler/moduleNameResolver.ts#L2130-L2185)), while non-ESM modes can subsequently append extensions to the original candidate ([resolution flow](https://github.com/microsoft/TypeScript/blob/b465fdbfe175304d9b977da137b2c178ae1091d3/src/compiler/moduleNameResolver.ts#L2064-L2105)).

Without `resolveJsonModule`, an explicit `.json` request can progress to appended `.json.js` and `.json.jsx` candidates ([trace fixture](https://github.com/microsoft/TypeScript/blob/b465fdbfe175304d9b977da137b2c178ae1091d3/tests/baselines/reference/requireOfJsonFileWithoutResolveJsonModuleAndPathMapping.trace.json#L8-L16)). Ancestor `node_modules` lookup searches TypeScript/declaration files before a separate JavaScript fallback pass ([trace](https://github.com/microsoft/TypeScript/blob/b465fdbfe175304d9b977da137b2c178ae1091d3/tests/baselines/reference/requireOfJsonFileWithoutResolveJsonModuleAndPathMapping.trace.json#L21-L35), [implementation](https://github.com/microsoft/TypeScript/blob/b465fdbfe175304d9b977da137b2c178ae1091d3/src/compiler/moduleNameResolver.ts#L2997-L3043)).

## Package subpaths

For `dependency/subpath`, TypeScript first identifies `dependency` as the package root and looks for a manifest at the physical subpath. It uses that nested manifest only when the dependency root has no `exports` map; a root map is authoritative ([5.5.2](https://github.com/microsoft/TypeScript/blob/v5.5.2/src/compiler/moduleNameResolver.ts#L3054-L3130), [6.0.3](https://github.com/microsoft/TypeScript/blob/v6.0.3/src/compiler/moduleNameResolver.ts#L3091-L3152)).

The nested manifest contributes `types`/`typings` and `main`, not `exports`. The resolver tries those package fields before an index candidate; an index is only a fallback outside ESM mode ([5.5.2](https://github.com/microsoft/TypeScript/blob/v5.5.2/src/compiler/moduleNameResolver.ts#L2484-L2545), [6.0.3](https://github.com/microsoft/TypeScript/blob/v6.0.3/src/compiler/moduleNameResolver.ts#L2489-L2551)). This is compiler resolution behavior, not a claim that Node ESM supports folder mains.
